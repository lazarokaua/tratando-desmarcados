[project]
name = "tratando_desmarcados"
version = "0.1.0"
description = "Basic data analysis project template"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pandas",
    "streamlit",
    "openpyxl"
]

[tool.uv]
# Managed by uv for environment configuration
